name: gobbetly
description: A web app that uses AngularDart Components
# version: 1.0.0
# homepage: https://www.example.com
# author: stoke <email@example.com>

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  angular: ^5.0.0
  angular_components: ^0.9.1
  angular_router: ^2.0.0-alpha+19
  firebase: ^5.0.0
  googleapis_auth: 0.2.5+2
  service_worker: ^0.2.3

dev_dependencies:
  angular_test: ^2.0.0
  build_runner: ^0.10.0
  build_test: ^0.10.2
  build_web_compilers: ^0.4.0
  test: ^1.0.0
  sass_builder: ^2.0.0
  mockito: ^3.0.0-beta+1
  pageloader: ^3.0.0
